package com.app.questr.model.enums;

/**
 * Categories a task can belong to.
 * Used for dashboard breakdowns and filtering.
 */
public enum TaskCategory {
    WORK,
    PERSONAL,
    HEALTH,
    LEARNING,
    DEV,
    OTHER
}

